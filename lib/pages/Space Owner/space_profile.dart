import 'package:flutter/material.dart';

class SpaceProfile extends StatefulWidget {
  const SpaceProfile({super.key});

  @override
  State<SpaceProfile> createState() => _SpaceProfileState();
}

class _SpaceProfileState extends State<SpaceProfile> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
